name: output info
on: 
  issues:
    types: opened
  workflow_dispatch:
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: output github context
        run: |
         echo "${{ github.event.issue.labels }}"
    
